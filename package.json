{
  "name": "whatsapp-gatekeeper",
  "version": "1.0.0",
  "description": "WhatsApp AI Gatekeeper — Claude-powered message filtering and smart replies",
  "type": "module",
  "module": "src/index.ts",
  "scripts": {
    "dev": "bun --watch run src/index.ts",
    "start": "bun run src/index.ts",
    "test": "bun test"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "qrcode-terminal": "^0.12.0",
    "whatsapp-web.js": "^1.26.1-alpha.3"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/qrcode-terminal": "^0.12.0",
    "typescript": "^5.9.3"
  }
}
